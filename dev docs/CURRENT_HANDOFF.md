# 当前交接

> 本文件只维护任务续接与带日期的生产快照；源码能力和自动验证的唯一来源为 [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md)。

## 任务续接

- 更新：2026-09-14。LLM Token 自动化源码已部署（`09e4c6a`、`4226541`）：`0023` 已为 head，Grounded Map/Delta/Retry Guard/Router/Soft Budget/POI Gate/Anomaly/Regression 均已加载。部署前 active Job/lease=0、`integrity_check=ok`，备份 `data/backups/app-pre-llm-token-automation-20260914-143324.db` 完整性 ok；API/Worker/首页/心跳 READY。
- 已修复：LaunchAgent 原传 `ZHIJIAN_ENV` 而配置读取 `ENV`，曾启动 development 并生成 2 条无来源演示 Job；已改为 `ENV=production`、删除该两条本次生成的空 Job 并重装，复核未再生成。真实本地 `qwen3.5:9b` JSON 连通通过；主远程 Profile 返回 429，未重试。
- 已部署（2026-09-15）：内容、视频笔记、任务、来源统一支持多选批删；工具条位于列表标题栏，复选框不再与图标重叠，长标题/路径省略。来源批删跳过有关联内容/笔记/活跃任务的记录，证据链删除经确认后同步清理关联内容和视频笔记。部署前 revision `0023`、`integrity_check=ok`、active Job/lease=0，备份 `data/backups/app-pre-bulk-delete-route-20260915-221000.db` 完整性 ok；重启后 API/Worker/心跳 READY，OpenAPI 已含四个批删路由。后端 162 项、Ruff、Node 24 前端 verify 与 `diff --check` 通过；未执行真实删除。
- 已部署（2026-09-15）：设置页模型下拉不再把名称、分隔点和模型 ID 的 React 子节点以逗号串接；`Free SN GLM · glm-5.2` 等标签恢复正确显示，不改变已保存 Profile 或 Provider 调用。部署前 revision `0023`、`integrity_check=ok`、active Job/lease=0，备份 `data/backups/app-pre-select-label-20260915-231000.db` 完整性 ok；重启后 API/Worker/心跳 READY，首页加载新 `index-oRXPhXJZ.js`。前端 lint、17 项测试、TypeScript、Vite build 与 `diff --check` 通过；浏览器控制通道和项目 Playwright 均不可用，未取到修复后的页面截图。
- 已部署（2026-09-15）：设置页阶段标题现仅显示中文业务名；`GROUND_MAP` 为“证据地图”，不再拼接 `STRUCTURED_EXTRACTION` 等技术能力码。部署前 revision `0023`、`integrity_check=ok`、active Job/lease=0，备份 `data/backups/app-pre-stage-labels-20260915-233000.db` 完整性 ok；重启后 API/Worker/心跳 READY，首页加载新 `index-prYIMaWe.js`。前端 lint、17 项测试、TypeScript、Vite build 与 `diff --check` 通过；浏览器控制通道和项目 Playwright 均不可用，未取到修复后的页面截图。
- 已完成、未部署（2026-09-16）：`MODEL_RELIABILITY_PLAN.md` 已实现。Model Profile JSON Setting 支持四种可靠性模式与高级覆盖；调用层处理限速、并发、429 退避、quota 熔断、JSON 恢复、结构化校验和同 endpoint 不同模型 fallback，Audit 保持脱敏。完整后端回归、Ruff、Node 24 前端 verify、`diff --check` 通过；设置页本地桌面/390px 预览及控制台通过。未调用真实 Provider/视频，未迁移或重启；生产加载前不得将模拟结果描述为真实稳定性验收。
- 已修复、未部署（2026-09-16）：现场 `job_ea308f2be1424feeb7446462d527694e` 的失败根因是截断 v1 Ground Map Cache 被 Replay 连续命中，并非 Replay 新调用模型失败。Cache Hit 现先校验；非法项记录 `SKIPPED / AI_CACHE_INVALID` 后绕过，合法 v1 项继续复用，新结果写 v2 且保留 `previous_entry_id`。完整后端回归、Ruff、`diff --check` 通过；未删除缓存、未调用 Provider、未自动 Replay。部署后仅需从 `EXTRACT_TRAVEL_FACTS` 续跑，无需重做下载/ASR/校对。
- 首次部署复核（2026-09-16）：Cache 修复加载后 API READY，但 Worker 因 `providers.llm → ai.reliability → ai.__init__ → gateway/cache → providers.llm` 循环导入退出；此前旧 heartbeat 不构成 Worker READY 证据。未领取 Job、未修改数据库；该次重启不计为完成部署。
- 已部署（2026-09-16）：已将 `AIWorkloadGateway` 改为 `zhijian.ai` 的惰性导出并新增 Worker import 回归。第二次重启前 active Job/lease=0、生产 `integrity_check=ok`、revision=`0023`；备份 `data/backups/app-pre-worker-cycle-fix-20260916-095516.db` 完整性 ok、revision=`0023`。重启后 API/Worker `RUNNING`、API content/Worker heartbeat `READY`，heartbeat PID=`38268` 与 Worker LaunchAgent 一致；生产 Python 与 `PYTHONPATH=.../backend/src` 已实读。完整后端回归、Ruff、Node 24 前端 verify 与 `diff --check` 通过。未迁移、未删除旧 Cache、未调用 Provider、未自动 Replay；如需处理该失败 Job，仅从 `EXTRACT_TRAVEL_FACTS` 续跑。
- 已配置并真实 Replay（2026-09-16）：`TRANSCRIPT_CORRECTION` 设为 `REMOTE_ONLY`、GLM Stage retry=1；`Free SN GLM / glm-5.2` 使用 GUARDED、2.5 秒间隔、单并发、HTTP/JSON 各 1 次、熔断 3 次/60 秒；转写分块调整为 8000 字符/32 Segment。备份 `data/backups/app-pre-glm-only-replay-20260916-141025.db` 完整性 ok、revision=`0023`，无 migration；重启后 API/Worker/心跳 READY。`job_9a47f904736844bebd1e8e0049795d98` 仅从 `CORRECT_TRANSCRIPT` Replay 一次，上游 6 步 REUSED；首批 32 Segment 即返回 429 `insufficient_quota / inference exceeds tpm/rpm limit`，被分类为 `AI_PROVIDER_QUOTA_EXHAUSTED` 并立即 OPEN，未同模型重试、未调用 Ollama。任务停在 FAILED；不得继续请求，需额度窗口恢复或更换远程 Profile 后再决定是否 Replay。
- 已修复并部署（2026-09-16）：附件事件 `evt_50aa33a4545f45dc9ff2906a6eba374c` 对应后续 GLM Replay，首批 32 Segment/4047 输入字符在 65 秒后以 `finish_reason=length` 截断，触发 `AI_PROVIDER_OUTPUT_TRUNCATED`。转写配置已进一步收紧为 4000 字符/16 Segment；代码在输出截断时将当前批次二分并串行继续，不原样重试大批次。完整后端回归、Ruff、`diff --check` 通过；备份 `data/backups/app-pre-transcript-split-20260916-142848.db` 完整性 ok、revision=`0023`。重启后 API/Worker/内容/心跳 READY，heartbeat PID=`56133` 与 LaunchAgent 一致；未迁移、未自动 Replay、未调用 Provider。
- 已优化并部署（2026-09-16）：GLM 转写校对现在对子批成功结果立即提交，未返回的 Segment 记为 `UNCHANGED`，后续 Replay 跳过已完成段；截断学习到的安全批量按 model 保存在 Job runtime hint。结构化错误不再额外消耗 HTTP retry；TPM/RPM 临时限制独立为 `AI_PROVIDER_THROTTLED` 并仅熔断该 model，DIRECT 付费 Profile 不继承等待、锁、重试或 circuit；Provider 阻塞的 Replay 转为 `NEEDS_USER`。完整后端 180 项与 Ruff 通过。重启前 active Job/lease=0、`integrity_check=ok`、revision=`0023`；备份 `data/backups/app-pre-reliability-isolation-20260916-continue.db` 完整性 ok。无 migration 重启后 API/Worker/内容/心跳 READY，heartbeat PID=`60964` 与 LaunchAgent 一致，active Job/lease=0；未自动 Replay、未调用真实 Provider。
- 已修复并部署（2026-09-16）：`job_00923d8d98fb412ab71eef27d98f188a` 的 OpenRouter 首次失败实际为账户/Guardrail ZDR 策略排除 9/9 免费端点，旧分类误作网络错误并重试成泛化 400。现返回不可重试的 `AI_PROVIDER_POLICY_BLOCKED` 与明确处理提示；转写 Job 保留具体 Provider 错误码。日志事件详情新增“复制日志”。后端 181 项、Ruff、前端 verify、`diff --check` 通过；桌面/390×844 Browser 验收通过且控制台无 warning/error。部署前 active Job/lease=0、`integrity_check=ok`、revision=`0023`，备份 `data/backups/app-pre-openrouter-policy-log-copy-20260916.db` 完整性 ok；无 migration 重启后 API/Worker/内容/心跳 READY，heartbeat PID=`69757` 与 LaunchAgent 一致。未自动重跑或调用 Provider；OpenRouter 账户级 ZDR 仍需用户调整或改用支持 ZDR 的模型。
- 已修复并部署（2026-09-16）：完整重跑现在复用原 Job ID；终态任务直接清空执行态并 `QUEUED`，运行中任务先协作式取消，Worker 释放 lease 后重入队同一 Job，保留 Job ID/SystemEvent/ExternalCallAudit、重置 Step 执行态和中间 Artifact，且不创建替代任务。任务列表运行态右侧改为显示“正在〈实际阶段〉”；失败卡长 Provider URL 在 390px 下可换行，无横向溢出。后端 182 项、Ruff、前端 verify、`diff --check` 通过；桌面/390×844 Browser 验收通过且控制台无 warning/error。部署前 active Job/lease=0、`integrity_check=ok`、revision=`0023`，备份 `data/backups/app-pre-task-replay-mobile-overflow-20260916.db` 完整性 ok；无 migration 重启后 API/Worker/内容/心跳 READY，heartbeat PID=`71298` 与 LaunchAgent 一致。未触发真实模型/视频重跑。
- 已修复并部署（2026-09-16）：任务列表统一了可删除与运行中行的左右槽位，运行中行不显示假复选框但保留同宽空位，标题、阶段、进度和状态列不再错位。前端 verify 与 `diff --check` 通过；970px/390×844 Browser 无横向溢出且控制台无 warning/error。部署前 active Job/lease=0、`integrity_check=ok`、revision=`0023`，备份 `data/backups/app-pre-task-row-alignment-20260916.db` 完整性 ok；无 migration 重启后 API/Worker/内容/心跳 READY，heartbeat PID=`81734` 与 LaunchAgent 一致。未触发真实模型/视频重跑。
- 已修复并部署（2026-09-17）：`job_8711f9326c474cae87a3d3c083401c55` 的本地 Ground Map 实际在 `7970+222=8192` Token 处截断；付费 DeepSeek fallback 的 4096 Completion 全被默认 thinking 消耗，正文为 0，旧逻辑又误报“空内容”。现将 Profile 上下文传给 Ollama、向 DeepSeek 官方接口落实 Stage thinking 开关，空正文但长度结束统一归类截断，转写与 Ground Map 均可二分恢复。生产审计未发现其他阻塞型截断；Note 已有确定性兜底。后端 189 项、Ruff、Node 24 前端 verify、`diff --check` 通过。部署前 active Job/lease=0、`integrity_check=ok`、revision=`0023`，备份 `data/backups/app-pre-truncation-chain-fix-20260917-235300.db` 完整性 ok；无 migration 重启后 API/Worker/内容/心跳 READY，heartbeat PID=`96671`。该 Job 的 24 小时 Replay Artifact 已过期，未绕过门禁、未调用真实 Provider；生产 E2E 需新任务或明确授权完整重跑。
- 未完成/边界：没有合规 10/30/60 分钟视频，WP15 真实毕业、远程成功/fallback、Vision、ASR corpus 和 390px Browser 仍未验证。真实删除结果尚未取证；不得重跑既有媒体/ASR/校对或自动确认 POI。

## 生产快照（采样 2026-09-13）

- 2026-09-13 已重启 cn.zhijian.api、cn.zhijian.worker；health、首页正文和 Worker 心跳 READY。迁移前 active Job/lease=0，SQLite/WAL `integrity_check=ok`。
- SQLite/WAL 实读 Alembic 0022（head），`video_note_search` 已存在；本次升级前逻辑备份为 `data/backups/app-pre-ui-video-poi-v2-20260913-125600.db`，完整性为 ok。
- 运行时为 Python 3.14.6，/Volumes/D/Library/Application Support/Zhijian/venv/bin/python；LaunchAgent 的 PYTHONPATH 指向仓库 backend/src。精确 Git SHA 未嵌入进程。
- 2026-09-13 修复后再次重启；API/Worker/首页/心跳 READY，生产 `integrity_check=ok`、active Job/lease=0。真实样本已证明下载、ASR、校对、抽取、Note、POI Review、截图和 FTS 交付；仍不外推为 Vision、Profile 切换、fallback、cache/force-regenerate、预算或 Browser/390px 验收。
- 2026-09-14：Evidence Index 改为稳定 Mention ID 顺序，部署后真实验证 COMPACT Profile、Cache Hit 与 force-regenerate；API/Worker/心跳 READY。Local Video 只验证至 ASR，后续本地校对超时；本轮不再重试。
- 2026-09-14（本次复核）：`data/app.db` revision 为 `0023`，`integrity_check=ok`，未见 active Job/lease；API/Worker 与心跳 READY。本次没有重启，以上只证明迁移后的存储和现有运行状态，不证明新源码已加载。
- 2026-09-14（本次部署）：先备份 `app-pre-llm-token-automation-20260914-143324.db` 并验证 ok，revision `0023` 无需重复迁移；重装后 API/Worker/首页/心跳 READY，LaunchAgent 环境为 `ENV=production`，`grounded_map_artifacts` 表存在。主远程 Profile 返回 429，未重试；本地 JSON 测试通过。
- 2026-09-16（本次部署）：本轮仅重启加载当前 checkout，未迁移；重启前 active Job/lease=0、`integrity_check=ok`、revision=`0023`。备份 `app-pre-cache-validation-20260916-095211.db` 已验证完整性/revision。重启后 API/Worker `RUNNING`、API content 与 Worker heartbeat `READY`；LaunchAgent 生产 Python/PYTHONPATH 已实读核对。未触发真实 Provider、视频续跑或 Cache 删除。
- 2026-09-16（部署修正）：首次重启后 Worker 因新可靠层触发的包循环导入退出，故上述首次 Worker READY 已撤回；修复 `zhijian.ai` Gateway 惰性导出、完整回归和第二份备份后重新重启。API/Worker 均 RUNNING，heartbeat PID=`38268` 已与 Worker 进程核对一致；未迁移或触发真实视频/Provider。
- 2026-09-16（GLM-only 实验）：备份与闲置/完整性门禁通过后写入 REMOTE_ONLY/GLM GUARDED/32 Segment 配置并受控重启；真实 Replay 未进入内容生成，首个校对批次即被 SenseNova 以 `insufficient_quota` 429 拒绝。可靠层未重试 quota、未切本地模型；API/Worker/心跳继续 READY，active Job/lease=0。
- 2026-09-16（截断修复部署）：后续 Replay 已越过 429，但 GLM 首批输出达到长度上限；未继续自动请求。部署批次二分恢复与 4000/16 配置前备份 `app-pre-transcript-split-20260916-142848.db` 并验证 ok；无 migration。部署后 API/Worker RUNNING、内容/心跳 READY、active Job/lease=0。
- 2026-09-16（增量恢复与隔离部署）：部署前 active Job/lease=0、`integrity_check=ok`、revision=`0023`，备份 `app-pre-reliability-isolation-20260916-continue.db` 验证 ok；无 migration。重启后 API/Worker RUNNING、内容/心跳 READY，heartbeat PID=`60964` 与 LaunchAgent 一致，active Job/lease=0。未触发真实 Provider 或 Replay。
