# Codex 低额度维护规则

适用于本仓库所有 Codex 任务。目标是减少重复读取、无关工具输出和重复验证，不降低正确性、安全性与证据要求。

## 默认接手顺序

1. 先读 `dev docs/CODEX_CONTEXT.md`。
2. 再读 `dev docs/IMPLEMENTATION_STATUS.md` 和 `dev docs/REGRESSION_AND_CHANGE_GUARD.md` 中与当前任务有关的部分。
3. 只读取一个直接相关的专项规格；被其明确引用且确有必要时再读下一级文件。
4. 默认禁止读取 `dev docs/COMPLETE_PROJECT_SPEC.md`。仅在用户明确要求全局审计、重新生成合订本或检查跨域冲突时读取。

## 上下文与工具输出

- 先 `rg` 定位，再用窄范围 `sed`；禁止无目的输出整个文件、整个目录或完整数据库记录。
- 单次代码/文档输出优先控制在 200 行以内；SQL 查询只返回聚合和必要样本。
- Browser/Computer Use 只读取目标页面、目标容器和相关控制台错误；禁止默认导出完整 DOM、AX Tree、全页日志或多张重复截图。
- 图片默认使用满足判断所需的最低 detail；只有像素级设计审阅才使用原始尺寸。
- 不在同一任务重复读取已确认内容；后续只查变化和失败证据。

## 工作包边界

- 一个任务只处理一个明确工作包：诊断、规格、后端、前端或验收不要无界扩张。
- 用户给出宽泛请求时，以当前冻结版本和直接阻塞项为范围；`FUTURE_ROADMAP.md` 不自动授权实施。
- 先冻结验收条件，再写代码。已满足验收时停止，不追加“顺手优化”。
- 诊断请求只读并报告；未明确要求修复时不改代码。

## 模型与推理档建议

- 文档整理、检索、Lint、小型样式：优先高效模型，低或中推理。
- 常规功能、测试、局部重构：优先成本/能力平衡模型，中推理。
- Job 状态机、Alembic、Replay、Evidence、安全边界：才使用旗舰模型和高推理。
- 不默认使用 max/ultra/pro；只有代表性任务证明质量提升值得额外额度时启用。

模型选择由用户/客户端控制；本文件只规定选择原则，不自动切换模型。

## 验证策略

1. 开发中先跑目标单测、目标 Lint/类型检查。
2. 完成工作包后只跑一次全量后端和前端验证。
3. 只有前端行为改变才用 Browser；只有本机 App 交互必须验证时才用 Computer Use。
4. 不以测试为由触发真实外部 LLM、视频重新生成、任务重跑或 Provider 真实测试；这些需用户明确要求或专项验收授权。
5. 重启服务前先确认无活跃 Job；文档-only 变更不重启服务。

## 项目硬约束

- 长任务必须进入 Worker；API 请求内不跑 ASR、LLM 或媒体下载。
- 新 Schema 必须新建 Alembic migration；禁止修改已发布历史迁移。
- 不破坏 Source/Snapshot/Segment/Claim/Evidence、Job/Step/Artifact、note/ntv、raw/corrected Transcript 身份边界。
- Secret 不进入 SQLite 明文、日志、URL、导出或 localStorage。
- 不把 Fixture、概念稿或构建成功描述为真实端到端验收。
- 不在没有证据时宣称完成；保留明确的未验证项。

## 最终交付

- 结果优先，保持紧凑；只报告变更、验证、已知限制和必要下一步。
- 不复述工具过程，不粘贴大段日志，不重复已经在 commentary 中确认的内容。
