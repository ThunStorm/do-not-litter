# Codex 低额度维护规则

适用于本仓库所有 Codex 任务。使用紧凑排版。目标是减少重复读取、无关工具输出和重复验证，不降低正确性、安全性与证据要求。

## 默认接手顺序

1. 项目任务先读 `dev docs/CODEX_CONTEXT.md`；已在当前上下文加载则不重复。只介绍项目时，读根 `README.md` 即可停止。
2. 开发/诊断再读 `dev docs/IMPLEMENTATION_STATUS.md`，并用 `rg` 定位 `dev docs/REGRESSION_AND_CHANGE_GUARD.md` 的相关条目。用户要求继续上次任务时读 `dev docs/CURRENT_HANDOFF.md` 的任务续接段；部署/服务问题才读其中的生产快照。
3. 按接手页选择一个专项文件/分篇的相关章节；索引只是导航，不意味着必须读取全部链接。首轮文档读取目标不超过约 12 KB（软预算，不是 token 配额），长行也计入；不能为了省额度跳过安全与验收约束。
4. 当已经能说明目标、当前状态、影响代码、约束和验收方法时停止文档探索，转到目标代码。证据不足再说明缺口并增读一个相关文件；不要反复扫描目录。
5. 默认不读 `dev docs/history/`、旧计划正文、`dev docs/planning/`、未来路线、设计截图、合订本。仅在用户指定或有直接证据缺口时读取必要部分；这些内容不自动授权实施。
6. `dev docs/COMPLETE_PROJECT_SPEC.md` 仅在用户明确要求全局审计、完整导出或检查跨域冲突时按需读取；生成该文件不需要先读取它。

前端页面布局、组件、样式、表单、地图覆盖物变更前，按需阅读 `dev docs/design/FRONTEND_VISUAL_DESIGN_SYSTEM.md`。不得新增裸原生 Dropdown、私有按钮体系或未登记的颜色/字号；受影响的 PC 与 Mobile 断点必须 Browser 验收。

## 文档唯一来源

- 外部概览在根 `README.md`，Agent 路由在 `CODEX_CONTEXT.md`，全目录与拆合规则在 `dev docs/README.md`；三者不复制完整需求或状态。
- 当前源码能力/自动验证只更新 `IMPLEMENTATION_STATUS.md`；任务续接和生产现场分段写入 `CURRENT_HANDOFF.md`，现场注明原采样日期；历史记录只进 `dev docs/history/`，不能当作当前待办。
- 专项文件说明契约，不证明已实现；后出现的文件也不自动覆盖冻结需求。冲突先核对相关源码/测试与冻结清单，涉及需求选择时请用户确认。
- 拆分后的主题索引保留；纯跳转页在更新仓库内引用后删除，不双份维护正文。仅在生成脚本 source set 变化或其中源文档变化时重建合订本。

## 写交接

- 用户说“写交接”“准备换任务”时，更新 `dev docs/CURRENT_HANDOFF.md` 的任务续接段，不新建 HANDOFF_日期 等重复文件；不自动每轮写交接，用户要求验收后再写时须等待。
- 任务续接段目标不超过 600 字，只写：目标与范围、已完成及验证入口、未完成/阻塞、下一步一项、相关文件、禁止事项/待用户确认。明确是否提交、部署、真实验收；不复制项目介绍、规格正文或完整日志。
- 用覆盖方式淘汰已过期的任务续接内容，保留仍有效的约束与未闭环项；仅将有独立追溯价值的证据追加到已有 history 文档。
- 没有重新读取生产现场时，保留原采样日期并标注“本次未复核”，不把旧健康结果改成当前结果；写交接不授权重启、迁移、真实模型调用、提交或推送。

## 上下文与工具输出

- 先 `rg` 定位，再用窄范围 `sed`；禁止无目的输出整个文件、整个目录或完整数据库记录。
- 单次代码/文档输出优先控制在 200 行以内；SQL 查询只返回聚合和必要样本。
- Browser/Computer Use 只读取目标页面、目标容器和相关控制台错误；禁止默认导出完整 DOM、AX Tree、全页日志或多张重复截图。
- 图片默认使用满足判断所需的最低 detail；只有像素级设计审阅才使用原始尺寸。
- 不在同一任务重复读取已确认内容；后续只查变化和失败证据。

## 工作包边界

- 一个任务只处理一个明确工作包：诊断、规格、后端、前端或验收不要无界扩张。
- 用户给出宽泛请求时，以当前冻结版本和直接阻塞项为范围；`planning/FUTURE_ROADMAP.md` 不自动授权实施。
- 先冻结验收条件，再写代码。已满足验收时停止，不追加“顺手优化”。
- 诊断请求只读并报告；未明确要求修复时不改代码。

## 模型与推理档建议

- 文档整理、检索、Lint、小型样式：优先高效模型，低或中推理。
- 常规功能、测试、局部重构：优先成本/能力平衡模型，中推理。
- Job 状态机、Alembic、Replay、Evidence、安全边界：才使用旗舰模型和高推理。
- 不默认使用 max/ultra/pro；只有代表性任务证明质量提升值得额外额度时启用。

模型选择由用户/客户端控制；本文件只规定选择原则，不自动切换模型。

## 验证策略

1. 开发中先跑目标单测、目标 Lint/类型检查。
2. 代码工作包完成后只跑一次全量后端和前端验证；纯文档工作只检查链接、正文迁移完整性、生成一致性和 `git diff --check`，不跑业务全量测试。
3. 只有前端行为改变才用 Browser；只有本机 App 交互必须验证时才用 Computer Use。
4. 不以测试为由触发真实外部 LLM、视频重新生成、任务重跑或 Provider 真实测试；这些需用户明确要求或专项验收授权。
5. 重启服务前先确认无活跃 Job；文档-only 变更不重启服务。

## 项目硬约束

- 长任务必须进入 Worker；API 请求内不跑 ASR、LLM 或媒体下载。
- 新 Schema 必须新建 Alembic migration；禁止修改已发布历史迁移。
- 不破坏 Source/Snapshot/Segment/Claim/Evidence、Job/Step/Artifact、note/ntv、raw/corrected Transcript 身份边界。
- Secret 不进入 SQLite 明文、日志、URL、导出或 localStorage。
- 不把 Fixture、概念稿或构建成功描述为真实端到端验收。
- 不在没有证据时宣称完成；保留明确的未验证项。

## 最终交付

- 结果优先，保持紧凑；只报告变更、验证、已知限制和必要下一步。
- 不复述工具过程，不粘贴大段日志，不重复已经在 commentary 中确认的内容。
