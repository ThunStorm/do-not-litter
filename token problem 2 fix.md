## 一、项目内 API 额度消耗

### 1. 转写校对切分过碎，是最大消耗源

当前参数：

- `TRANSCRIPT_CORRECTION_CHUNK_CHARS = 2400`
- `TRANSCRIPT_CORRECTION_BATCH_SIZE = 16`

实际代码先按字符分块，随后又强制每 16 个 Segment 调一次模型。Whisper 的 Segment 很短，导致每次请求正文不多，却反复发送 System Prompt、JSON Schema、Segment ID 和响应结构。

当前 7 个视频的最新转写：

| 视频 Segment 数 | 正文字数 | 校对请求数 |
| --------------- | -------- | ---------- |
| 272             | 2,956    | 17         |
| 256             | 3,019    | 16         |
| 292             | 3,257    | 19         |
| 826             | 7,280    | 52         |
| 355             | 3,914    | 23         |
| 225             | 2,882    | 15         |
| 232             | 2,048    | 15         |

仅转写校对最低需要约 157 次模型调用；再加视频总结和地点提取，至少约 171 次逻辑调用。

这是明显的不合理拆分：例如 2,048 字被拆成 15 次请求，平均每次只有约 137 字正文，请求固定开销反而占主导。

### 2. 一次逻辑调用最多被放大为 6 次请求

当前默认策略：

- 主模型最多尝试 3 次：首次 + 2 次重试；
- 主模型失败后调用备用模型；
- 备用模型同样最多尝试 3 次。

因此一个逻辑调用最坏会产生 6 次 HTTP 请求。

按当前 7 个视频计算：

- 正常最低：约 171 次；
- 不考虑递归拆分的理论最大：约 1,026 次传输尝试。

如果结构化 JSON 解析失败，校对代码还会把一个批次递归二分。一个 16 Segment 批次理论上最多展开为 31 个子调用；叠加重试后会进一步放大。

### 3. 主模型与备用模型使用同一供应商

当前路由：

- 主模型：SenseNova `sensenova-6.8-flash-lite`
- 备用模型：SenseNova `deepseek-v4-flash`
- Base URL 都是 `https://token.sensenova.cn/v1`

这意味着发生供应商级 429、账户级限流或网络故障时，切换备用模型通常仍命中同一限流域，无法真正容灾，却会继续消耗请求次数。

本地审计已经记录：

- 模型重试 16 次；
- 其中转写校对 5 次、视频总结 5 次、地点提取 6 次；
- 主要原因是 `429 Too Many Requests` 和读取超时；
- 历史备用切换 21 次；
- 模型真实测试 20 次；
- 模型路由修改 33 次。

### 4. 1 秒请求间隔对免费接口过于激进

当前配置：

- 请求间隔 1 秒；
- 429/超时最多重试 2 次；
- 重试间隔 5 秒。

转写校对连续发送 15–52 个批次时，很容易触发免费接口限流。一旦第一个批次出现 429，后续批次继续发送、重试和切备用，形成调用放大。

### 5. 当前审计无法核算真实 token 和费用

这是另一个关键问题。

`LLMResult` 已取得 Provider 返回的 `usage`，但没有写入数据库。`ExternalCallAudit` 目前只覆盖：

- Bilibili 元数据；
- yt-dlp；
- Whisper ASR。

它没有包裹 LLM 请求，也没有记录：

- prompt tokens；
- completion tokens；
- cached tokens；
- Provider/Model；
- attempt；
- Job ID；
  -估算费用。

因此目前无法从本地数据库算出真实模型额度，只能确认调用次数和放大机制。精确消耗需要再对照 SenseNova、DeepSeek 等供应商后台账单。

历史任务被删除后，部分任务级审计也随之消失，进一步降低了追溯能力。

## 二、建议的治理优先级

### P0：先补项目 LLM 费用审计

每次模型尝试必须记录：

- Job ID、Step、Role；
- Provider、Model；
- primary/fallback；
- attempt；
- 输入字符数；
- prompt/completion/cached tokens；
  -耗时、状态码、错误；
  -供应商返回的 usage；
  -可选的费用估算。

不记录 Prompt 正文和 API Key。

在费用审计完成前，不应继续凭感觉调整模型。

### P0：重新设计转写校对批次

建议取消固定 16 Segment 上限，改为：

- 以字符/token 数为主；
- 每批上限 64–128 Segment；
- 目标约 1,500–2,400 中文字符；
- 保留 Segment ID 和顺序校验。

按当前数据估算：

- 现在：157 次校对调用；
- 64 Segment：约 41 次；
- 128 Segment：约 22 次；
- 加上总结/地点后，整体最低调用可从约 171 次降至约 36–55 次。

预期可减少约 68%–79% 的逻辑调用；具体质量需要 Fixture 验证。

### P0：修正重试与备用策略

- 429 优先读取 `Retry-After`；
- 首次 429 后暂停整个 Provider，而不是每个分块各自重试；
- 主/备用 Base URL 相同视为同一故障域，不执行无意义切换；
- 免费 Provider 默认最多 1 次重试；
- 连续 429 触发短期 Circuit Breaker；
- 超时和结构错误分开处理；
- 递归二分设置总调用上限。

### P1：调整模型分工

可考虑：

- 本地 Ollama：转写校对；
- 外部模型：最终摘要和地点提取；
- 强模型只处理高价值 Review/失败分块。

这样能把最多的 15–52 次校对请求移到本机，只保留少量外部调用。

### P1：减少模型真实测试

目前有 20 次模型测试。建议：

- 保存设置不自动测试；
- 同配置短时间内复用最近测试结果；
- 测试按钮防重复点击；
- 同一 Provider 只做最小一次响应；
- 显示上次测试时间和结果。
